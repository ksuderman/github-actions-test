name: Caller
on:
  workflow_dispatch:
    inputs:
      condition:
        type: boolean
        default: true

jobs:
  thething:
    uses: ./.github/tasks/called.yml
    with:
      condition: ${{ inputs.condition }}
